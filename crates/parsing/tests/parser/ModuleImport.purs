module ModuleImport where

import Prelude
import Data.List
import Value (value)
import Class (class Class)
import Type (List)
import DataList (Data(A, B))
import DataAll (Data(..))
import Prim as Prim
